# ---------------------------------------------------------------------------
# 이 파일은 backend 폴더를 빌드 문맥으로 사용할 때 적용됩니다.
# .gitignore 와 별개로 동작하므로 민감한 파일을 여기에도 반드시 적어야 합니다.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# 환경 변수
#
# 이미지에 키가 포함되지 않도록 제외합니다.
# 실제 값은 배포 시 환경 변수로 주입합니다.
# ---------------------------------------------------------------------------
.env
.env.local
*.env
.env.example

# ---------------------------------------------------------------------------
# 데이터베이스
#
# 기동 시 시드에서 다시 생성되므로 포함하지 않습니다.
# ---------------------------------------------------------------------------
*.db
*.sqlite
*.sqlite3
*.db-journal

# ---------------------------------------------------------------------------
# 양자화 전 모델
#
# 서비스는 양자화된 모델만 사용합니다. 원본을 넣으면 이미지가 불필요하게 커집니다.
# ---------------------------------------------------------------------------
app/sentiment/model_binary/nsmc_koelectra_small.onnx
app/sentiment/model/*.pth
app/sentiment/model/*_clean.onnx

# ---------------------------------------------------------------------------
# 개발 도구
#
# 실행에 필요하지 않은 실험 및 변환 스크립트입니다.
# ---------------------------------------------------------------------------
fetch_movies.py
export_binary_model.py
compare_models.py
test_binary_models.py
check_mismatch.py
validate_ensemble.py

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---------------------------------------------------------------------------
# 기타
# ---------------------------------------------------------------------------
.git
.gitignore
.dockerignore
Dockerfile
*.log
.vscode/
.idea/